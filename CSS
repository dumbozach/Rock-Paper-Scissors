html {
  height: 100%;
  width: 100%;
}

body {
  background-color: #333;
}

button {
  transition:all 0.15s linear 0s;
  border-radius:25px;
  background-color:#555;
}

button:hover{
  box-shadow: 5px 5px black;
  transition:box-shadow 0.15s linear, height 0.15s linear, width 0.15s linear;
}

#submit {
  font-size: 50px;
  border-radius:15px;
  text-align: center;
  font-family:OCR A Std, monospace;
}

h1 {
  color:white;
}
